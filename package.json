{
  "name": "telemetry-github",
  "version": "0.0.31",
  "description": "telemetry for GitHub client apps",
  "repository": {
    "url": "https://github.com/atom/telemetry"
  },
  "main": "./lib/index.js",
  "types": "./index.d.ts",
  "files": [
    "lib",
    "src",
    "index.d.ts"
  ],
  "scripts": {
    "info": "npm-scripts-info",
    "build": "run-s clean && run-p build:*",
    "build:main": "tsc -p tsconfig.json",
    "watch": "run-s clean build:main && run-p \"build:main -- -w\" \"test:unit -- --watch\"",
    "test": "run-s build test:*",
    "test:unit": "mocha --require mock-local-storage --project tsconfig.json --require async-dump.js --compilers ts:ts-node/register ./test/*.spec.ts",
    "test:lint": "tslint --project .",
    "cov": "run-s build test:unit cov:html && opn coverage/index.html",
    "cov:html": "nyc report --reporter=html",
    "cov:send": "nyc report --reporter=lcov > coverage.lcov && codecov",
    "cov:check": "nyc report && nyc check-coverage --lines 100 --functions 100 --branches 100",
    "doc": "run-s doc:html && opn build/docs/index.html",
    "doc:html": "typedoc src/ --target ES6 --mode file --out build/docs",
    "doc:json": "typedoc src/ --target ES6 --mode file --json build/docs/typedoc.json",
    "version": "standard-version -t ''",
    "reset": "git clean -dfx && git reset --hard && yarn i --prefer-offline",
    "clean": "trash lib test/*.js dist/*.tgz",
    "all": "run-s reset test cov:check doc:html",
    "prepare-release": "run-s all version doc:publish",
    "package": "yarn pack && mv telemetry-github-*.tgz dist/",
    "prepublish": "run-s version && git push --follow-tags && run-s build && run-s package",
    "publish": "release-it"
  },
  "scripts-info": {
    "info": "Display information about the package scripts",
    "build": "Clean and rebuild the project",
    "fix": "Try to automatically fix any linting problems",
    "test": "Lint and unit test the project",
    "watch": "Watch and rebuild the project on save, then rerun relevant tests",
    "cov": "Rebuild, run tests, then create and open the coverage report",
    "doc": "Generate HTML API documentation and open it in a browser",
    "doc:json": "Generate API documentation in typedoc JSON format",
    "version": "Bump package.json version, update CHANGELOG.md, tag release",
    "reset": "Delete all untracked files and reset the repo to the last commit",
    "prepare-release": "One-step: clean, build, test, publish docs, and prep a release"
  },
  "author": {
    "name": "GitHub, Inc.",
    "email": "atom@github.com"
  },
  "license": "MIT",
  "dependencies": {
    "uuid": "^3.3.2"
  },
  "optionalDependencies": {
    "lokijs": "^1.5.5"
  },
  "devDependencies": {
    "@bitjson/npm-scripts-info": "^1.0.0",
    "@types/chai": "^4.1.4",
    "@types/chai-as-promised": "^7.1.0",
    "@types/lokijs": "^1.5.2",
    "@types/mocha": "^5.2.5",
    "@types/sinon": "^5.0.2",
    "@types/uuid": "^3.4.3",
    "chai": "^4.1.2",
    "chai-as-promised": "^7.1.1",
    "codecov": "^3.0.4",
    "mocha": "^5.2.0",
    "mock-local-storage": "^1.0.5",
    "npm-run-all": "^4.1.3",
    "nyc": "^13.0.1",
    "sinon": "^6.1.5",
    "standard-version": "^4.4.0",
    "trash-cli": "^1.4.0",
    "ts-node": "^6.2.0",
    "tslint": "^5.11.0",
    "tslint-config-prettier": "^1.15.0",
    "typedoc": "^0.12.0",
    "typescript": "^2.9.2"
  },
  "nyc": {
    "exclude": [
      "**/*.spec.js"
    ]
  }
}
